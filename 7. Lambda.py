# Normal function
def h(x): # Makes a function named h that takes one input, x
    return x**4; # It returns x squared (x multiplied by itself).
print(h(6)) # Calls the function with 6, so it calculates 6x6x6x6 = 1296 and prints it

# Lambda Function / expressions 
han = lambda var:var*10
han(10)